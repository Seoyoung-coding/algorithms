# 219 슬라이딩 윈도우 개념
문제 해석 = 정수 배열 nums와 정수 k가 주어졌을 때, 배열 안에서 서로 다른 두 인덱스 i와 j가 존재해서
nums[i] == nums[j] 이고 |i - j| <= k 를 만족하면 true를 반환하라.

abs(i - j) <= k 의 의미는 index 차이가 k 이하인 범위 안에서만 비교하면 됨
index 기준으로 k칸 이내만 보고 싶기 때문에 -> k칸 밖의 숫자들은 아예 제거한다
nums = [1,2,3,1], k = 2
